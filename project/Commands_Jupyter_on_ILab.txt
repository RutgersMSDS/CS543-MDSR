# For help visit
# https://resources.cs.rutgers.edu/docs/new-users/introduction/


# This command has to be executed first in every putty session
export PYSPARK_PYTHON=/koko/system/anaconda/envs/python38/bin/python3

# This command has to be executed only once in the ilab lifetime
# export PATH="$PATH:/koko/system/anaconda/bin"

# This command activates python 3.8
source activate python38

# This command tells the current version of python activated
conda env list

# Note: Always make sure you are in your net-ID folder
cd /ilab/users/<net-id>/

# This commands starts jupyter notebook server your ilab machine
jupyter notebook --browser='none' --ip=`hostname`


# So the list of commands for continuing user:

export PYSPARK_PYTHON=/koko/system/anaconda/envs/python38/bin/python3
source activate python38
conda env list
cd /ilab/users/<net-id>/
jupyter notebook --browser='none' --ip=`hostname`


# And the list of commands for the first time user:

export PATH="$PATH:/koko/system/anaconda/bin"
export PYSPARK_PYTHON=/koko/system/anaconda/envs/python38/bin/python3
source activate python38
conda env list
cd /ilab/users/<net-id>/
jupyter notebook --browser='none' --ip=`hostname`
